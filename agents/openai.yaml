interface:
  display_name: "ML Paper Copilot"
  short_description: "Standalone two-phase paper workflow for ML, bioinformatics, and computational biology papers."
  default_prompt: "Use $ml-paper-copilot for self-contained paper drafting. Collect my venue, figures, results, and .bib in Phase 0, then run the bundled end-to-end paper workflow in Phase 1."

policy:
  allow_implicit_invocation: true
